{{/*
This is a placeholder template for secrets.
In production, use one of these approaches:

1. Create secrets manually before helm install:
   kubectl create secret generic agent-system-secrets \
     --namespace agent-system \
     --from-literal=SECRET_KEY=your-jwt-secret \
     --from-literal=ENCRYPTION_KEY=your-fernet-key \
     --from-literal=DATABASE_PASSWORD=your-db-password \
     --from-literal=NEO4J_AUTH='neo4j/your-neo4j-password'

2. Use sealed-secrets or external-secrets operator

3. Use Azure Key Vault with CSI driver

NOTE: OPENAI_API_KEY is NOT required at the system level.
      All users must configure their own API keys via the UI.

Example secret structure (DO NOT commit real values):

apiVersion: v1
kind: Secret
metadata:
  name: agent-system-secrets
  namespace: agent-system
type: Opaque
stringData:
  SECRET_KEY: "your-jwt-secret-key-min-32-chars"
  ENCRYPTION_KEY: "your-fernet-encryption-key"
  DATABASE_PASSWORD: "your-database-password"
  NEO4J_AUTH: "neo4j/your-neo4j-password"
*/}}
